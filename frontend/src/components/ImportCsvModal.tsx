import React from 'react';

export const ImportCsvModal = () => {
  return (
    <button className="brutal-btn" style={{ background: 'var(--white)', fontSize: '20px' }}>
      Import_CSV_Data
    </button>
  );
};