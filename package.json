{
  "name": "betterme-drone-delivery",
  "version": "1.0.0",
  "description": "Unified Drone Delivery Tax System",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "cd backend && npm install && cd ../frontend && npm install --include=dev && npm run build",
    "start": "cd backend && node_modules/.bin/ts-node src/index.ts",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build:frontend": "cd frontend && npm run build",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "setup": "npm run install:all && npm run build:frontend"
  }
}
